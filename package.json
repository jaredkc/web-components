{
  "name": "web-components",
  "version": "1.0.0",
  "description": "TailwindCSS for preview presentation",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tailwindcss -i ./assets/tailwind.css -o ./assets/tailwind.build.css -w",
    "build": "tailwindcss -i ./assets/tailwind.css -o ./assets/tailwind.build.css",
    "serve": "vite",
    "preview": "vite preview"
  },
  "author": "Jared Cornwall",
  "license": "ISC",
  "dependencies": {
    "@tailwindcss/typography": "^0.5.15",
    "tailwindcss": "^3.4.10"
  },
  "devDependencies": {
    "vite": "^7.1.10"
  }
}
